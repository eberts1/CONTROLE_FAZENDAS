export { PrismaClient } from '@prisma/client';
export type {
  User,
  Farm,
  FarmUser,
  Area,
  Process,
  ProcessRecord,
  Role,
  FarmRole,
  AreaType,
  ProcessCategory,
} from '@prisma/client';
